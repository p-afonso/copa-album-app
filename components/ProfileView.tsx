export function ProfileView(_props: { username: string; onUsernameChange: () => void }) {
  return <div style={{ padding: 24, color: 'var(--text-muted)', textAlign: 'center' }}>Perfil — em breve</div>
}
