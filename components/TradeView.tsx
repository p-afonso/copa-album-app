export function TradeView(_props: { albumId: string; userId: string; marketplaceVisible: boolean }) {
  return <div style={{ padding: 24, color: 'var(--text-muted)', textAlign: 'center' }}>Trocas — em breve</div>
}
